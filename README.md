# TextLoop
TextLoop is a text-first reminder app. The user creates reminders in a simple web app, and TextLoop sends those reminders by SMS. The user can then reply to the text with actions like DONE, SNOOZE, or CANCEL, and the backend updates the reminder's status and history.
